// Managed Functions: the API lives at the same origin under /api/*, and the
// Static Web App's own login gate (staticwebapp.config.json) protects it.
// No separate URL or key needed at all.
const API_BASE_URL = "/api";
